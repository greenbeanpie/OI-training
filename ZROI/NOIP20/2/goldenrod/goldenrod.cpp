#include <bits/stdc++.h>
using namespace std;
#define int long long

#ifdef CODESPACE
#pragma GCC optimize(2)
#endif
const int N = 1e5 + 5;

namespace goldenrod
{
	struct scanline
	{
		int x1, x2, y1, y2;
	} s[N];

	int tempy[N << 1], tempx[N << 1];

	signed main()
	{

		return 0;
	}
}
signed main()
{
	freopen("goldenrod.in", "r", stdin);
	freopen("goldenrod.out", "w", stdout);
	ios::sync_with_stdio(false), cin.tie(nullptr), cout.tie(nullptr);
	goldenrod::main();
	return 0;
}
