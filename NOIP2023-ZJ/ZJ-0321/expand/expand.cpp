#include<bits/stdc++.h>
using namespace std;
int main()
{
	freopen("expand.in ","r",stdin);
	freopen("expand.out","w",stdout);
	int c, m, n, q;
	cin >> c >> m >> n >> q;
	for (int i = 1; i <= q + 1; i++)
	{
		int ans = rand() % 2;
		cout << ans;
	}
	return 0;
}
