#include <bits/stdc++.h>
using namespace std;
const int N=1e3,M=1e5;
class Node {
public:
	int x,y,v;
} a[M+1];
int f[N+1][N+1];
int main() {
	freopen("run.in","r",stdin);
	freopen("run.out","w",stdout);
	int c,t;
	cin>>c>>t;
	srand(time(0));
	while(t--) {
		int n,m,k,d;
		cin>>n>>m>>k>>d;
		while(m--)
			cin>>a[i].x>>a[i].y>a[i].v;
		cout<<rand()*rand()<<endl;
	}
	return 0;
}